spring:
  application:
    name: sms-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848  #nacos 服务地址
        namespace: b488795a-11c7-4363-aed1-a47e9c14a3de  #nacos 命名空间id
        group: dev  #分组
        file-extension: yml  # 扩展名